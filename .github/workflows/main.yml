name: Hello World Training workflow

on:
    workflow_dispatch:
jobs:
      greet:
        runs-on: ubuntu-latest
        steps:
          - name: Greet The user
            run: echo "Hello World!"
          - run: ls -l
          - uses: actions/checkout@v2
          - runs: ls -l
          
